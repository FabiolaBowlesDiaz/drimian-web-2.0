---
phase: 01-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/Section.astro
  - src/components/Nav.astro
  - src/components/Footer.astro
  - src/components/Button.astro
  - src/components/Card.astro
  - src/pages/index.astro
autonomous: false
requirements:
  - FOUN-02
  - FOUN-03
  - FOUN-06
  - FOUN-08

must_haves:
  truths:
    - "Dark-themed and light-themed sections are visually distinct on the same page"
    - "Test page renders correctly at 375px mobile and 1440px desktop"
    - "Text content is constrained to max-width 680px within sections"
    - "Section spacing is 80-120px on desktop and 48-64px on mobile"
    - "Nav is hidden at page top and slides in when scrolling past hero area"
    - "Primary button is filled orange with white text, secondary is outlined"
    - "Cards have subtle 1px border, no shadow"
    - "Page weight of the shell is under 100KB"
  artifacts:
    - path: "src/components/Section.astro"
      provides: "Section wrapper with data-theme attribute for dark/light theming"
      exports: ["Props: theme, id, class, wide"]
    - path: "src/components/Nav.astro"
      provides: "Scroll-triggered navigation with IntersectionObserver"
      contains: "IntersectionObserver"
    - path: "src/components/Footer.astro"
      provides: "Site footer component"
    - path: "src/components/Button.astro"
      provides: "Primary/secondary button with variant prop"
      exports: ["Props: variant, href, class"]
    - path: "src/components/Card.astro"
      provides: "Card with subtle border, no shadow"
    - path: "src/pages/index.astro"
      provides: "Demo page showcasing all components in dark/light sections"
  key_links:
    - from: "src/components/Section.astro"
      to: "src/styles/global.css"
      via: "data-theme attribute triggers CSS custom property scoping"
      pattern: "data-theme"
    - from: "src/components/Nav.astro"
      to: "index.astro"
      via: "nav-sentinel element placed after hero section"
      pattern: "nav-sentinel"
    - from: "src/pages/index.astro"
      to: "src/components/Section.astro"
      via: "Multiple Section components with alternating themes"
      pattern: "theme=\"dark\"|theme=\"light\""
---

<objective>
Build all layout components (Section, Nav, Footer, Button, Card) and create a demo page that demonstrates the complete design system: hybrid dark/light sections, responsive layout at 375px and 1440px, scroll-triggered nav, and brand typography in action.

Purpose: These components are the building blocks for every page in Phase 2-5. The demo page serves as a visual reference and verification that the design system works end-to-end.
Output: A working demo page at localhost showing all components, ready for visual verification.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -- design tokens the executor needs -->

From src/styles/global.css (created in Plan 01):
```css
/* Section theme variables set by data-theme attribute */
[data-theme="dark"]  -> --section-bg, --section-text, --section-text-secondary, --section-border, --section-card-bg
[data-theme="light"] -> same variables, white variant
[data-theme="light-alt"] -> same variables, gray (#F5F5F5) variant

/* Spacing tokens */
--spacing-section-desktop: 6rem;   /* 96px */
--spacing-section-mobile: 3.5rem;  /* 56px */

/* Color utilities available as Tailwind classes */
bg-navy, bg-blue, bg-orange, bg-orange-hover
text-navy, text-blue, text-gray, text-orange
text-text-primary, text-text-secondary, text-text-muted (dark surfaces)
text-text-dark, text-text-body (light surfaces)
bg-bg-dark, bg-bg-section, bg-bg-card, bg-bg-white, bg-bg-light

/* Border radius */
rounded-sm (4px), rounded-md (8px), rounded-lg (12px)
```

From src/layouts/Base.astro (created in Plan 01):
```astro
---
interface Props {
  title: string;
  description?: string;
}
---
<!-- Provides: <html lang="es">, font preloads, global CSS import, <body><slot /></body> -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Section, Button, Card, and Footer components</name>
  <files>src/components/Section.astro, src/components/Button.astro, src/components/Card.astro, src/components/Footer.astro</files>
  <action>
1. Create src/components/Section.astro:
   - Props: { theme: 'dark' | 'light' | 'light-alt'; id?: string; class?: string; wide?: boolean }
   - Renders a `<section>` element with:
     - `data-theme={theme}` attribute (this triggers CSS custom property scoping from global.css)
     - Horizontal padding: px-6 md:px-12
     - Vertical padding using spacing tokens: py-[var(--spacing-section-mobile)] md:py-[var(--spacing-section-desktop)]
     - Background and text from section variables: bg-[var(--section-bg)] text-[var(--section-text)]
     - Inner div with max-w-[680px] mx-auto (unless `wide` prop is true, then max-w-5xl)
     - Accepts id and additional classes
   - Follow the EXACT pattern from 01-RESEARCH.md Pattern 3.

2. Create src/components/Button.astro:
   - Props: { variant?: 'primary' | 'secondary'; href?: string; class?: string }
   - Default variant: 'primary'
   - Base classes: inline-flex items-center gap-2 font-semibold text-base transition-all duration-300 cursor-pointer rounded-md
   - Primary: px-8 py-4 bg-orange text-white hover:bg-orange-hover hover:-translate-y-0.5
   - Secondary: px-8 py-4 border border-[var(--section-border)] text-[var(--section-text)] hover:border-[var(--section-text-secondary)]
   - Renders as `<a>` if href provided, `<button>` otherwise
   - Per user decision: "Primary filled orange (#FF6600) white text. Secondary outlined or text with arrow"

3. Create src/components/Card.astro:
   - Props: { class?: string }
   - Per user decision: "Subtle border (1px light gray), no shadow. Clean, defined edges"
   - Classes: p-6 md:p-7, border border-[var(--section-border)], rounded-lg, bg-[var(--section-card-bg)]
   - Slot for content

4. Create src/components/Footer.astro:
   - Simple footer component (dark theme)
   - Contains: Drimian copyright, "Dream with Intelligence" tagline
   - Uses dark surface colors (bg-bg-dark or bg-navy)
   - Centered text, small font size, muted text color
   - Padding consistent with section spacing
   - This is a minimal footer for now -- Phase 2 will add full contact info
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/OneDrive - theblankinc.com/Grupo Foianini/drimian-web2.0" && pnpm astro check 2>&1 | tail -5 && pnpm build 2>&1 | tail -3</automated>
  </verify>
  <done>All 4 component files exist with correct Props interfaces. Build succeeds with no type errors. Section accepts theme prop, Button has primary/secondary variants, Card has subtle border.</done>
</task>

<task type="auto">
  <name>Task 2: Create scroll-triggered Nav and demo page with all components</name>
  <files>src/components/Nav.astro, src/pages/index.astro</files>
  <action>
1. Create src/components/Nav.astro:
   - Per user decision: "Hero has NO nav bar. Nav appears on scroll down (slides in from top), hidden when user is at the top"
   - Per user decision: "Nav links: Logo (left) + 'Principios' link + 'Empezar diagnostico' CTA button (orange)"
   - Per user decision: "Mobile: hamburger menu (standard)"

   Structure:
   - `<nav id="main-nav">` with fixed positioning, z-50, starts with -translate-y-full (hidden)
   - transition-transform duration-300 for smooth slide-in
   - Background: bg-navy/95 backdrop-blur-sm (per research recommendation -- semi-transparent navy works on both dark/light sections)
   - Desktop: flex layout with logo left, links + CTA right
   - Logo: `<img src="/images/logo-white.png" alt="Drimian" class="h-8" />` (file may not exist yet, that's fine)
   - "Principios" link: text-sm font-medium, hover:text-orange
   - "Empezar diagnostico" button: inline-flex bg-orange text-white text-sm font-semibold rounded-md px-6 py-2.5
   - Mobile: hamburger button (md:hidden), simple toggle for mobile menu

   - Include `<div id="nav-sentinel" class="h-0 w-0" aria-hidden="true"></div>` OUTSIDE the nav (this gets placed in the page after the hero)

   - Inline `<script>` for IntersectionObserver (NOT a Preact island -- zero JS bundle):
     ```javascript
     const nav = document.getElementById('main-nav');
     const sentinel = document.getElementById('nav-sentinel');
     if (nav && sentinel) {
       const observer = new IntersectionObserver(
         ([entry]) => {
           if (!entry.isIntersecting) {
             nav.classList.remove('-translate-y-full');
           } else {
             nav.classList.add('-translate-y-full');
           }
         },
         { threshold: 0 }
       );
       observer.observe(sentinel);
     }
     ```

   - Mobile menu toggle script (inline, ~10 lines):
     Toggle visibility of mobile menu panel on hamburger click.

   NOTE: The nav-sentinel must be placed in the PAGE, not in Nav.astro, because it needs to sit at the bottom of the hero section. Nav.astro only contains the nav bar itself. Add a comment in Nav.astro explaining this.

2. Update src/pages/index.astro to be a DEMO PAGE showcasing all components:

   Import: Base, Section, Nav, Footer, Button, Card

   Page structure (demonstrates hybrid dark/light theme with hard cuts):

   a. Nav component (fixed, hidden initially)

   b. DARK section (id="hero"): Simulates hero area
      - Large headline in Playfair Display: "Dream with Intelligence" using text-hero class
      - Subtitle in Inter: "Consultoria estrategica basada en decisiones"
      - Primary Button: "Empezar diagnostico"
      - Secondary Button: "Conocer mas"
      - Must be tall enough (min-h-screen) so scrolling past it triggers the nav

   c. nav-sentinel div (placed right after hero section)

   d. LIGHT section (id="demo-light"): Typography showcase
      - h2: "Typography Scale"
      - Demonstrate h1, h2, h3 sizes
      - Body text paragraph at max-width 680px
      - Show text-body and text-small sizes

   e. LIGHT-ALT section (id="demo-cards"): Card showcase
      - h2: "Card Components"
      - 2-3 Card components with sample content
      - Cards in a grid (1 col mobile, 2 cols md)

   f. DARK section (id="demo-dark"): Dark theme showcase
      - h2: "Dark Theme Section"
      - Body text showing --section-text-secondary
      - A Card on dark background
      - Primary and Secondary buttons

   g. LIGHT section: Spacing verification
      - h2: "Spacing System"
      - Text explaining the spacing tokens being used

   h. Footer component

   Per user decisions:
   - Hard cuts between sections (no gradients, no dividers)
   - "Confident + minimal. Lots of whitespace. Few elements, each with weight."
   - "Animations: Almost none." -- Only add a simple fade-in-on-scroll if trivial, otherwise skip for now
   - Orange is ONLY for CTAs/action items
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/OneDrive - theblankinc.com/Grupo Foianini/drimian-web2.0" && pnpm build 2>&1 | tail -3 && echo "--- Page weight check ---" && du -sh dist/ && ls -la dist/_astro/ 2>/dev/null | head -10</automated>
  </verify>
  <done>Build succeeds. Demo page renders with: (1) dark hero section with dramatic headline, (2) light typography section, (3) light-alt cards section, (4) dark theme section, (5) footer. Nav slides in on scroll. All components use design system tokens. Total dist/ output under 100KB.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of design system</name>
  <action>User verifies the complete design system visually at desktop and mobile breakpoints.</action>
  <what-built>Complete design system foundation: Astro 5 project with brand colors, Playfair Display + Inter fonts, responsive layout components (Section, Nav, Button, Card, Footer), and a demo page showing hybrid dark/light sections with all components.</what-built>
  <how-to-verify>
    1. Run `pnpm dev` in the project directory
    2. Open http://localhost:4321 in browser
    3. Verify at DESKTOP (1440px):
       - Hero section: dark background, large serif headline (~72px), orange CTA button
       - Scroll down: nav slides in from top with "Principios" link and orange "Empezar diagnostico" button
       - Light sections: white and gray (#F5F5F5) alternating, with clear hard cuts (no gradients)
       - Cards: subtle border, no shadow
       - Dark section below: distinct dark background with light text
       - Section spacing: generous whitespace between sections (~96px)
       - Text constrained to ~680px width
    4. Verify at MOBILE (375px in DevTools):
       - Hero headline scales down (~38px)
       - Sections stack cleanly with no horizontal overflow
       - Spacing reduces (~56px between sections)
       - Nav shows hamburger icon on mobile
    5. Check DevTools > Network > filter "woff2":
       - 3 font files load (playfair-display-600, playfair-display-700, inter-latin-var)
       - Headlines render in Playfair Display (serif), body in Inter (sans-serif)
    6. Overall feel: "Clean y sofisticada" -- confident, minimal, lots of whitespace, BCG/Bain energy
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with colors, typography, spacing, or overall feel</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm build` exits 0 with no errors
2. All 5 components exist: Section.astro, Nav.astro, Footer.astro, Button.astro, Card.astro
3. Demo page shows dark/light/light-alt sections with distinct visual treatment
4. Nav is hidden at top, slides in on scroll (IntersectionObserver)
5. Responsive: no horizontal overflow at 375px, proper spacing at both breakpoints
6. Page weight under 100KB (dist/ output)
7. Zero JS bundles -- only inline scripts for nav behavior
</verification>

<success_criteria>
- All layout components render correctly with design system tokens
- Hybrid dark/light sections are visually distinct with hard cuts between them
- Demo page is responsive from 375px to 1440px with correct spacing
- Nav appears on scroll, hidden at top
- Buttons show primary (filled orange) and secondary (outlined) variants
- Cards have subtle border, no shadow
- Total page weight of shell under 100KB
- User approves the visual feel at the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-02-SUMMARY.md`
</output>
