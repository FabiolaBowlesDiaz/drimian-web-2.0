---
phase: 02-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets/images/drimian-logo.png
  - src/data/principles.ts
  - src/components/Hero.astro
  - src/components/WhatsAppFloat.astro
  - src/styles/global.css
autonomous: true
requirements: [HOME-01, HOME-06]

must_haves:
  truths:
    - "Hero section fills viewport with dark theme, shows logo, headline, subtitle, and two CTAs"
    - "Floating WhatsApp button is visible bottom-right on all pages, links to wa.me with pre-built message"
    - "Anchor scroll accounts for fixed nav height (no content hidden under nav)"
  artifacts:
    - path: "src/assets/images/drimian-logo.png"
      provides: "Optimized logo for Astro Image component"
    - path: "src/data/principles.ts"
      provides: "10 Guia Drimian principles with month-based rotation function"
      exports: ["Principle", "principles", "getCurrentPrinciple"]
    - path: "src/components/Hero.astro"
      provides: "Full-viewport dark hero with logo, headline, subtitle, 2 CTAs"
      min_lines: 20
    - path: "src/components/WhatsAppFloat.astro"
      provides: "Fixed-position WhatsApp button with inline SVG icon"
      min_lines: 10
  key_links:
    - from: "src/components/Hero.astro"
      to: "src/assets/images/drimian-logo.png"
      via: "Astro Image import"
      pattern: "import.*astro:assets"
    - from: "src/components/WhatsAppFloat.astro"
      to: "https://wa.me/59176600056"
      via: "href attribute"
      pattern: "wa\\.me/59176600056"
---

<objective>
Set up image assets, principle data, Hero section, and floating WhatsApp button -- the foundational pieces that Plan 02 will compose into the full homepage.

Purpose: Hero is the first thing visitors see and sets the tone. WhatsApp float is the persistent conversion path. Logo and principles data are shared assets needed by multiple components.
Output: Hero.astro, WhatsAppFloat.astro, drimian-logo.png in src/assets, principles.ts data file, scroll-padding fix in global.css
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-homepage/02-CONTEXT.md
@.planning/phases/02-homepage/02-RESEARCH.md

<interfaces>
<!-- Existing components the executor will use -->

From src/components/Section.astro:
```astro
interface Props {
  theme: 'dark' | 'light' | 'light-alt';
  id?: string;
  class?: string;
  wide?: boolean;
}
```

From src/components/Button.astro:
```astro
interface Props {
  variant?: 'primary' | 'secondary';
  href?: string;
  class?: string;
}
```

From src/styles/global.css (line 84-87):
```css
html {
  scroll-behavior: smooth;
  -webkit-font-smoothing: antialiased;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy logo and create principles data file</name>
  <files>src/assets/images/drimian-logo.png, src/data/principles.ts</files>
  <action>
**Logo setup:**
1. Create directory `src/assets/images/`
2. Copy logo from `C:/Users/Lenovo/OneDrive - theblankinc.com/Grupo Foianini/Drimina-logos/Drimian-1.png` to `src/assets/images/drimian-logo.png`
3. Visually inspect the logo PNG. It is 1600x397px RGBA with transparency. Determine if the logo elements (text + isotipo) are light-colored (visible on dark navy #131a37) or dark. If dark, note that CSS `filter: brightness(0) invert(1)` will be needed in Hero.astro.

**Principles data file:**
Create `src/data/principles.ts` with all 10 principles from the Guia Drimian. Each principle has: id (1-10), title, body (3-4 lines of practical application), and month (1-12, wrapping: month 11 = principle 1, month 12 = principle 2).

Use the 3 examples from the Documento Maestro Section 6 as the first 3 principles:
- Month 1: "El sistema produce resultados, no las personas." + body from spec
- Month 2: "Empujar mas fuerte no funciona." + body from spec
- Month 3: "La IA amplifica lo que ya tenes." + body from spec

For principles 4-10, derive titles and bodies from the 10 Guia Drimian principles listed in CLAUDE.md (the 10 operating principles). Keep the same provocative, practical tone as the 3 spec examples. Each body should be 2-3 sentences of practical application for a business owner.

Export:
```typescript
export interface Principle {
  id: number;
  title: string;
  body: string;
  month: number; // 1-12
}
export const principles: Principle[];
export function getCurrentPrinciple(): Principle;
```

The `getCurrentPrinciple()` function uses `new Date().getMonth()` (0-11) to select the current month's principle. Since Astro builds at deploy time, this selects at build time (which is the intended behavior -- rotation happens on redeploy).
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/OneDrive - theblankinc.com/Grupo Foianini/drimian-web2.0" && test -f src/assets/images/drimian-logo.png && node -e "import('./src/data/principles.ts').catch(() => process.exit(0))" && echo "Files exist"</automated>
  </verify>
  <done>Logo PNG exists in src/assets/images/. principles.ts exports 10 principles and getCurrentPrinciple function. Both files are valid.</done>
</task>

<task type="auto">
  <name>Task 2: Build Hero section and WhatsAppFloat components</name>
  <files>src/components/Hero.astro, src/components/WhatsAppFloat.astro, src/styles/global.css</files>
  <action>
**Hero.astro** -- Create `src/components/Hero.astro`:
- Import Section, Button, and `{ Image } from 'astro:assets'`
- Import logo from `'../assets/images/drimian-logo.png'`
- Wrap in `<Section theme="dark" id="hero" class="min-h-[100dvh] flex items-center">` (use 100dvh for mobile browser chrome fix, NOT min-h-screen)
- Center content vertically and horizontally
- Logo: `<Image src={logo} alt="Drimian - Dream with Intelligence" width={320} class="mx-auto mb-10" />` -- if logo is dark on transparent, add `class="... brightness-0 invert"` to make it white on dark background. Use discretion based on visual inspection from Task 1.
- Headline (h1 with text-hero class): "¿Estas construyendo lo que va a hacer prospera a tu empresa?" -- exact copy from spec. Use proper accents: "Estas" not "Estás" to match spec accent style already in CONTEXT.md.
- Subtitle: "10 preguntas. 3 minutos. Descubri si estas empujando mas fuerte -- o cambiando lo que importa." -- use em dash character (—) not double hyphen
- Primary CTA: `<Button variant="primary" href="#que-es-drimian">Empezar el diagnostico</Button>` (scrolls to next section for now; Phase 3 will change target)
- Secondary CTA: a simple `<a>` tag (not Button component) with subtle styling: `text-[var(--section-text-secondary)] hover:text-[var(--section-text)] text-sm` linking to `#que-es-drimian` with text "O segui leyendo ↓"
- NO decorative elements, NO isotipo watermark, NO animations. Clean dark navy background per user decision.

**WhatsAppFloat.astro** -- Create `src/components/WhatsAppFloat.astro`:
- WhatsApp number: 59176600056
- Pre-built message (Claude's discretion, natural conversational tone): "Hola, vi el diagnostico de Drimian y me gustaria conversar sobre mi empresa."
- Build URL: `https://wa.me/59176600056?text=${encodeURIComponent(message)}`
- Render as `<a>` with: `fixed bottom-6 right-6 z-40 flex items-center justify-center w-14 h-14 rounded-full bg-[#25D366] text-white shadow-lg hover:shadow-xl hover:scale-105 transition-all duration-200`
- Inline WhatsApp SVG icon (the standard WhatsApp path from the research document)
- `target="_blank" rel="noopener noreferrer" aria-label="Contactanos por WhatsApp"`

**global.css update** -- Add `scroll-padding-top: 5rem;` to the `html` rule in `@layer base` (line 85-88). This offsets anchor scroll targets by 80px (64px nav + 16px breathing room) so content is not hidden behind the fixed nav.
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/OneDrive - theblankinc.com/Grupo Foianini/drimian-web2.0" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Hero.astro renders full-viewport dark section with logo (via Astro Image), exact headline/subtitle copy, orange CTA + subtle secondary CTA. WhatsAppFloat.astro renders fixed green button bottom-right with correct wa.me link. global.css has scroll-padding-top. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds (no broken imports, no image resolution errors)
- `src/assets/images/drimian-logo.png` exists and is imported via Astro Image (not `<img>` tag)
- `src/data/principles.ts` exports 10 principles with getCurrentPrinciple function
- Hero uses `min-h-[100dvh]` not `min-h-screen`
- WhatsApp URL contains `wa.me/59176600056`
- global.css contains `scroll-padding-top`
</verification>

<success_criteria>
Build passes. Hero component and WhatsAppFloat component are ready for Plan 02 to compose into index.astro. Logo is optimized via Astro Image pipeline. Principles data is available for the RotatingPrinciple component.
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-01-SUMMARY.md`
</output>
